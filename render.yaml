services:
  - type: web
    name: hr-profile-tracker
    env: node
    plan: free
    buildCommand: npm run install-all && npm run build
    startCommand: npm run server
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000